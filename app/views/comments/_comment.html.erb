<div id="comment_<%= comment.id %>" class="comment border-b py-2">
  <p><%= comment.content %></p>
  <small class="text-gray-500"><%= comment.created_at.strftime("%Y/%m/%d %H:%M") %></small>
</div>
